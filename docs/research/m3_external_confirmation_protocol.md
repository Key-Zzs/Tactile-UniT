# M3 External Confirmation Protocol

Publication-level confirmation is pending. Before opening any future external
data, freeze a protocol based on
`configs/tactile_unit/m3_external_confirmation_protocol.json`.

The data must be fresh V+A+C bridge data never used by C1–C6 training or model
selection. It must preserve synchronized physical transitions, planned or
executed Action, Contact history, and the target-horizon contract. Reuse the
M3 alignment, full/fallback prediction, uncertainty, temporal sensitivity,
causal legality, and noncollapse definitions where applicable. Any necessary
domain adaptation must be fixed before inspection.
